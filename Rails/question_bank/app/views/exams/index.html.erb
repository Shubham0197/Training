<p style="color: green"><%= notice %></p>

<h1>Exams</h1>

<div id="exams">
  <% @exams.each do |exam| %>
    <%= render exam %>
    <p>
      <%= link_to "Show this exam", exam %>
    </p>
  <% end %>
</div>

<%= link_to "New exam", new_exam_path %>
