<h2>Update the Details of the aircraft</h2>
<%= form_with(model: [:admin,@aircraft]) do |f| %>
<%= f.text_field(:name) %>
<%= f.text_field(:cruising_range) %>
<%= f.submit("Update Details") %>
<% end %>