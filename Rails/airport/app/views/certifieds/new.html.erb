<h1>Certifieds#new</h1>
<%= form_with model: @certified do |form| %>
  <%= render "form_certifieds" , form: form%>
  <div class="mb-3">
    <%= form.submit "Certify Pilot", class: "btn btn-primary" %>
  </div>
<% end %>
